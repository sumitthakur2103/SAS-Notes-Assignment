# Notes Workspace - Backend

Simple Express + MongoDB backend for the AI Notes Workspace.

Setup:
1. Copy .env.example to .env and fill values (MONGO_URI, JWT_SECRET, GEMINI_API_KEY)
2. npm install
3. npm run dev

Deployment:
- Use Render for server deployment. Set environment variables on Render.
- Use MongoDB Atlas for database.
